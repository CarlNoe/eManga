{
    "devDependencies": {
        "@prettier/plugin-php": "^0.19.2",
        "husky": "^8.0.2",
        "prettier": "^2.8.1",
        "tailwindcss": "^3.2.4"
    },
    "scripts": {
        "prepare": "husky install",
        "pre-commit": "prettier --write . && git add -A ."
    }
}
